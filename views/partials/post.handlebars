<div class="row mb-4 list-group-item">
  <div class="col-md-5">
    <h2>
      <a class="post-title" id="{{id}}" href="/post/{{id}}">{{title}}</a>
    </h2>
    <p>
      {{content}}
    </p>
    <p>Created by {{user.name}} on {{format_date date_created}}</p>
  </div>
</div>